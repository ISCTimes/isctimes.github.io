/*
$(document).ready( () => {
	let navigationBarButton = $('#' + currentPage);
	if (navigationBarButton.length)
	{
		navigationBarButton.addClass("selected").attr("href", "javascript:void(0)");
	}
});
*/
